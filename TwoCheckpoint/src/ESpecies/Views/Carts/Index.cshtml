@model IEnumerable<ESpecies.Models.Carts>

@{
    ViewData["Title"] = "Index";
}

<h2>Cart</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EntityId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EntityId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ApplicationUserId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ApplicationUserId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ApplicationUserId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
